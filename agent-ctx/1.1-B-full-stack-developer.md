# Task 1.1-B — full-stack-developer

## Task
Construir tres secciones de Fase 1.1 en `src/components/rp/sections-fase1/f1-b.tsx`:
- `Fase1Logica` (id `f1-logica`, index 04) — Arquitectura lógica: 17 dominios, capas, tabla de dependencias permitidas, regla de inversión de dependencias.
- `Fase1Fisica` (id `f1-fisica`, index 05) — Arquitectura física sobre Cloudflare: Control Plane + Tenant Cells, tabla de servicios CF con límites y riesgo, callout D1 ~10GB, convenciones físicas.
- `Fase1Diagrama` (id `f1-diagrama`, index 06) — Diagrama general integrando request + tenant + datos + eventos, leyenda de flujo, stats SLO/RPO/RTO.

## Work Log
- Leí `worklog.md`: infra de Fase 1.1 ya lista (mermaid, primitives extendidas, phase-store, nav con 24 items, page.tsx que importa los 6 bloques A–F).
- Leí `primitives.tsx` y `mermaid.tsx` para conocer firma exacta y estilos disponibles (variantes de GlassCard, tonos de Pill, Risk levels, Callout kinds).
- Leí `f1-a.tsx` para alinear convenciones de import (`@/components/rp/primitives`), estructura y tono editorial.
- Verifiqué sintaxis mental de los 3 charts Mermaid:
  - Chart 1 (lógica): subgraphs con comas/paréntesis dentro de strings citadas, cylinders `[(...)]` para infra — válido.
  - Chart 2 (física): edge label `|route org→cell|` con unicode arrow, diamond `{"Org → Shard Router"}`, multi-line con `<br/>` — válido.
  - Chart 3 (diagrama): edge label citado `|"transacción: estado + outbox"|`, cylinder multi-line `D1[("D1 shard<br/>tenant cell")]`, re-referencia `DO --> D1` — válido.
- Construí `f1-b.tsx` con:
  - `DOMAIN_DEPS` (15 filas) con `id`, `dep`, `exp`, `note?` — Pills teal para "Expuesto a" con keys estables `${d.id}-exp-${token}`, nota en mono muted para anotaciones (Stripe/Google/Workers AI).
  - `CF_SERVICES` (10 filas) con `svc`, `rol`, `lim`, `risk` — `<Risk level={...} />` en cuarta columna, keys `${s.svc}-risk` etc.
  - GlassCard gold para "Regla de inversión de dependencias" con dos columnas: Principio + Casos de swap reales (NotificationSender, PaymentGateway, ChatModel/EmbeddingModel, ObjectStore).
  - Callout warn "D1 no es infinito": 10GB, sharding obligatorio para 100k, Time Travel ≠ backup, R2 con retención+cifrado.
  - GlassCard teal "Leyenda de flujo" con 5 invariantes (WAF→Auth→Tenant→RBAC→Entitlement, org_id servidor, outbox+queues, idempotencia, DO efímero vs D1 durable).
  - Stat row: latencia p95 < 300ms (gold), SLO 99.9% (teal), RPO ≤ 15min (fg), RTO ≤ 2h (fg).
- Lint: `bun run lint` → limpio, 0 errores.
- Dev log: "✓ Compiled in 546ms" — mi módulo resuelve correctamente. Errores residuales en log son de f1-c/d/e/f (otros agentes, paralelo), no míos.

## Stage Summary
- Archivo creado: `src/components/rp/sections-fase1/f1-b.tsx` (3 exports: `Fase1Logica`, `Fase1Fisica`, `Fase1Diagrama`).
- 3 diagramas Mermaid válidos (TB, TB, LR) integrados con el theme dark+gold+teal del proyecto.
- 2 DataTables con keys estables por celda (patrón `${id}-${field}` y `${id}-exp-${token}`) — cumple regla lint de keys únicas.
- 1 Callout warn (D1), 1 GlassCard gold (inversión de dependencias), 1 GlassCard teal (leyenda de flujo), 1 GlassCard default (convenciones físicas).
- 4 Stats (latencia/SLO/RPO/RTO) en grid responsive 2→4 columnas.
- Contenido 100% en español, técnicamente preciso sobre primitivas Cloudflare (D1 ~10GB, DO = unidad de partición, KV consistencia eventual, R2 egress gratuito, Time Travel = point-in-time no backup).
- Lint limpio. Sin blockers.
