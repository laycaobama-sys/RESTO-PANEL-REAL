# Task 1.1-D — full-stack-developer

## Task
Construir el bloque D de Fase 1.1 (Arquitectura Enterprise): autenticación y permisos, impersonación segura y estrategia de tiempo real, como 3 componentes exportados desde `src/components/rp/sections-fase1/f1-d.tsx`.

## Work Log
- Leí `worklog.md`: infra de Fase 1.1 lista (primitives extendidas con Risk/Callout/Code, Mermaid client con theme dark+gold+teal, phase-store, nav con 24 items, page.tsx como wrapper). Bloques A, B y C ya creados. page.tsx importa `Fase1Auth, Fase1Impersonation, Fase1Realtime` de `@/components/rp/sections-fase1/f1-d`.
- Revisé `primitives.tsx` para firmas exactas (Section, Tag, Risk, GlassCard variant=gold/teal/default, Stat accent=gold/teal/fg, Pill tone=teal, H3, Lead, DataTable head/rows, GoldList items, Callout kind=adr/warn/info/ok).
- Revisé `mermaid.tsx`: client, theme dark con primaryBorderColor #D4AF37 y lineColor #3DD6C9, fallback de error legible. securityLevel "loose".
- Revisé `f1-b.tsx` y `f1-c.tsx` para alinear patrones: imports vía `@/components/rp/primitives`, helper `Mono` para texto monoespaciado dorado, keys por fila del DataTable en estilo `key="rol"`, `key="scope"`, etc.
- Creé `src/components/rp/sections-fase1/f1-d.tsx` con tres exports:

### Fase1Auth (id=f1-auth, index=12, eyebrow="Autenticación y permisos")
- Title e intro exactos del brief.
- 4 Stats: Roles de sistema 12, Scopes 4, Factor por defecto Passkey, Política de cache Por request.
- DataTable "Roles del sistema" (12 filas × 4 cols: Rol | Scope | Descripción | Prioridad). Prioridad usa `<Tag kind="Imprescindible">` o `<Tag kind="Importante">` según corresponda. Scope usa `<Pill tone="teal">`.
- GlassCard gold "Roles personalizados" con GoldList (6 ítems): subconjunto de permisos, heredan constraints de scope, nunca más privilegio que el creador, auditados, versionados, aplicables por org/restaurant/location.
- DataTable "Permisos granulares (ejemplos)" (10 filas × 4 cols: Recurso | Acción | Scope | Notas). Recurso en mono gold, Acción en mono fg, Scope en Pill teal, Notas en texto muted.
- GlassCard default "Modelo RBAC + ABAC" con GoldList (5 ítems con spans keyificados): RBAC base, ABAC contextual, evaluación en servidor, deny por defecto, cache de permisos en memoria de request.
- Mermaid sequence diagram de auth flow (Usuario → Worker → Auth Middleware → Sesiones D1 → RBAC/ABAC → Dominio). Sintaxis `sequenceDiagram` con `participant` y flechas `->>` / `-->>`.
- GlassCard gold "MFA, Passkeys, recuperación" con GoldList (9 ítems): passkeys default, TOTP fallback, recovery codes cifrados, recuperación escalonada, revocación, rotación de tokens, API keys scoped, cuentas de servicio, separación interna/clientes.

### Fase1Impersonation (id=f1-impersonation, index=13, eyebrow="Impersonación segura")
- Title e intro exactos del brief con helper Mono para `entrar como…`.
- DataTable "Requisitos de impersonación" (12 filas × 3 cols: Requisito | Implementación | Riesgo). Riesgo usa `<Risk level>` con nivel apropiado (medio/alto/crítico según brief).
- Mermaid state diagram `stateDiagram-v2` con 8 transiciones entre Solicitada → Verificada → ActivaLectura ↔ ActivaEscritura → Revocada → [*]. Etiquetas entre comillas para evitar parse issues con `+` y `/`.
- GlassCard gold "Lo que la impersonación NUNCA permite" con GoldList (7 ítems): secretos, billing, eliminación, API keys, MFA del usuario real, banner, duración sin re-MFA.
- Callout kind="ok" title="Trazabilidad dual": explica actor_real + actor_efectivo, audit_log append-only inmutable, sesión propia del usuario impersonado no afectada.

### Fase1Realtime (id=f1-realtime, index=14, eyebrow="Estrategia tiempo real")
- Title e intro exactos del brief.
- DataTable "Casos de tiempo real" (7 filas × 4 cols: Caso | Unidad de partición (DO) | Estado canónico | Riesgo). Partición en mono fg, Canónico en mono gold, Riesgo con `<Risk level>` (bajo/medio/alto/crítico según brief).
- GlassCard gold "Relación DO ↔ D1" con GoldList (6 ítems): DO coordina concurrencia, mutación persistida en D1, reconstrucción desde D1, DO no es única copia, WebSockets con hibernación, reconexión con re-sync.
- Mermaid sequence diagram de prevención de doble reserva (Cliente 1 y 2 → DO → D1 → Queues). Muestra el happy path de C1 y el conflicto de C2.
- Callout kind="warn" title="Cloudflare Realtime, post-validación": Cloudflare Realtime (WebRTC/SGS) solo post-validación; base inicial es DO + WebSockets.
- GlassCard default "Recuperación tras desconexión" con GoldList (6 ítems): event_id, petición de delta, respuesta desde log efímero o D1, conflictos por D1, SyncStatus, backoff exponencial.

## Keys en DataTables
- Roles del sistema: `key="rol"`, `key="scope"`, `key="desc"`, `key="prio"` por fila (reusables entre filas, distintas dentro de la misma).
- Permisos granulares: `key="recurso"`, `key="accion"`, `key="scope"`, `key="notas"`.
- Requisitos de impersonación: `key="req"`, `key="impl"`, `key="risk"`.
- Casos de tiempo real: `key="caso"`, `key="particion"`, `key="canon"`, `key="risk"`.

## Mermaid
- Auth flow: `sequenceDiagram` con 6 participants y 11 flechas. Sin comillas en labels (sintaxis limpia).
- Impersonación: `stateDiagram-v2` con 8 transiciones; labels entre comillas para soportar `+`, `/` y `<5min`.
- Doble reserva: `sequenceDiagram` con 5 participants y 9 flechas. Muestra C1 happy path y C2 conflicto.

## Lint y dev log
- `bun run lint` → 0 errores, 0 warnings en f1-d.tsx.
- Dev log: solo reporta module-not-found para `f1-f` (otro agente paralelo). Mi archivo f1-d.tsx compila limpio.

## Stage Summary
- Archivo: `src/components/rp/sections-fase1/f1-d.tsx` (~470 líneas, 3 exports).
- 3 secciones navegables vía NAV_FASE1 (items 12, 13, 14).
- 4 DataTables (12 + 10 + 12 + 7 = 41 filas totales) con keys estables y únicas por celda.
- 3 diagramas Mermaid válidos (sequence, state, sequence) integrados con theme dark+gold+teal.
- 5 GlassCard (2 gold + 3 default), 1 Callout ok (trazabilidad dual), 1 Callout warn (Cloudflare Realtime post-validación), 4 Stats.
- Contenido 100% en español, técnicamente preciso sobre Cloudflare (DO = unidad de partición, hibernación reduce coste, D1 canónico, DO efímero, doble reserva con DO lock + D1 UNIQUE, Cloudflare Realtime post-validación, WebSockets vía DO).
- Lint limpio. Sin blockers para este bloque.
