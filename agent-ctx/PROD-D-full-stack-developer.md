# PROD-D — full-stack-developer

## Task
Construir el bloque D de Fase 5 (Producto) como 3 componentes interactivos en `src/components/rp/sections-producto/p-d.tsx`: `ProductoCRM`, `ProductoAutomatizaciones`, `ProductoReputacion`. Cada uno incluye componentes React con `useState` reales (no maquetas estáticas).

## Work Log
- Leí worklog.md previo: Fase 0 + Fase 1.1 + Fase 1.2 + Fase 4 completas y verificadas; infra Fase 5 (Producto) lista con phase-store de 5 fases (default `producto`), NAV_PRODUCTO 21 items, page.tsx que importa `ProductoCRM, ProductoAutomatizaciones, ProductoReputacion` desde `@/components/rp/sections-producto/p-d`. Directorio `sections-producto/` existente con `p-hero.tsx` ya creado por architect.
- Revisé `primitives.tsx` (firmas exactas: Section, Tag, Risk, GlassCard variant gold/teal/default/strong, Stat, Pill tone gold/teal/outline/default, H3, Lead, DataTable, GoldList items: ReactNode[], KV k+v, Callout kind adr/warn/info/ok, Code lang+children string). Revisé `p-hero.tsx` para alinear tono editorial y patrones (DEMO_BADGE amber, tokens rp-gold-text/rp-glass/rp-glow-gold/rp-scroll-thin).
- Verifiqué `lucide-react` instalado (^0.525.0) e importé 26 iconos operativos (Users, Star, Crown, Clock, Mail, Phone, Calendar, Gift, AlertTriangle, Filter, Send, Check, X, Zap, GitBranch, Hourglass, MousePointerClick, Play, Pause, Copy, Sparkles, MessageSquare, TrendingUp, ThumbsUp, ChevronRight).
- Creé `src/components/rp/sections-producto/p-d.tsx` (~1840 líneas, 3 exports):
  - **ProductoCRM** (id=p-crm, index 12, eyebrow "CRM y marketing"): intro cubre historial/frecuencia/ticket/preferencias/alergias/cumpleaños/tags/segmentos/VIP/loyalty/consents/notas/fuente. Componente interactivo con `useState(selectedId)`: lista de 6 clientes demo (Elena Marín VIP bajo, Javier Soler recurrente medio, Marta Iborra riesgo alto no-show, David Puig VIP cumpleaños, Lucía Ferrer nuevo medio, Andrés Vidal inactivo 90d alto) cada uno con nombre/visitas/VIP badge/risk badge. Click selecciona y pinta panel de detalle con avatar inicial, contacto (email/phone), LTV, visitas, mesa favorita, alergias, consentimientos (ConsentBadge email/whatsapp check/X), historial de 3-4 visitas (fecha/servicio/pax/ticket o "No-show" destructivo), tags (Pill con tone condicional), notas internas. Mini-section "Segmentos" con 4 segmentos (VIP 12, Inactivos 90d 184, Cumpleaños este mes 37, Riesgo no-show 9) cada uno con count y "ver →". Mini-section "Campañas" con 3 campañas (nombre/audiencia/estado Pill/CTR). DataTable "Datos del CRM" 15 filas con Risk en columna Sensible. GlassCard gold "Reglas de CRM" GoldList 6 (consent versionado, org_id forzado, consent+horario silencioso+cuota, crm.export audita, VIP/risk por eventos, fusión con auditoría). Callout warn "Consentimiento obligatorio" (nada sin consent, retira bloquea, no listas externas, no spam, consent por canal+finalidad versionado).
  - **ProductoAutomatizaciones** (id=p-automatizaciones, index 13, eyebrow "Builder de automatizaciones"): intro cubre triggers/conditions/actions/waits/branches/historial/logs/activar-pausar-duplicar + ejemplos. Componente interactivo con 3 useState: `nodes[]` (FlowNode con id/type/label/config), `selectedNode`, `active` (toggle), `duplicated`/`simulated` feedback. Layout 3 columnas: paleta izquierda (5 tipos Trigger/Condition/Action/Wait/Branch, click añade nodo al final del flujo); canvas central (flujo horizontal de cards con type icon, label, config preview, click para seleccionar, X para eliminar, ChevronRight entre nodos, scroll horizontal); panel lateral gold (configuración del nodo seleccionado con textarea editable). 5 plantillas (Recordatorio de reserva, Reconfirmación, Cumpleaños, Recuperación inactivos, Solicitud de reseña) cada una carga un flujo predefinido al click. Historial de ejecuciones 4 items (success/failed/pending con StatusDot animado pulse para pending). Controles superiores: Activar/Pausar toggle (cambia color/icono Play/Pause), Duplicar (feedback "Duplicado ✓" 1.8s), Simular (feedback "Simulación iniciada…" 2.4s + barra teal). DataTable "Componentes del builder" 15 filas (Trigger/Condition/Action/Wait/Branch/Plantilla/Variable dinámica/Límite por plan/Idempotencia/Anti-bucle/Versionado/Simulación/Pausa/Historial/Aprobación humana). GlassCard gold "Ejemplos de automatizaciones" GoldList 9 (recordatorio T-24h, reconfirmación T-2h, cumpleaños→cupón, recuperación inactivos 90d, solicitud reseña post-servicio, recuperación no-show, campaña temporada, aviso encargado si VIP reservada, incidencia si reseña negativa). Callout warn "Aprobación humana para lo sensible" (pricing/campañas públicas/respuestas reseñas/eliminación/pagos nunca auto-ejecutados).
  - **ProductoReputacion** (id=p-reputacion, index 14, eyebrow "Reputación, analytics e IA"): intro cubre reviews tray + AI replies + sentiment + star evolution + themes + comparativa + filtros + reply history + prior approval; analytics (ocupación/ingresos/ticket/reservas/cancelaciones/no-shows/nuevos/recurrentes/peak/forecast/comparativa/export); IA Copilot con queries tipo (cuántas reservas mañana, clientes en riesgo, mejor campaña, local más ingresos, horas menor ocupación, acciones para subir ticket medio) y siempre muestra fuente/fecha/confianza/acciones sugeridas/revisión antes de ejecutar/log de ejecutadas. Componente interactivo con 4 useState: `selectedReviewId`, `replyText`, `approved`, `chat[]` + `copilotInput`. Layout: lista de 5 reseñas (Carlos 5★ positive sin responder, Nuria 4★ neutral sin responder, Anónimo 2★ negative sin responder, Sergio 5★ positive respondida, Pilar 3★ neutral sin responder) cada una con stars + snippet + SentimentPill + replied status. Click selecciona y muestra panel gold con reseña completa + análisis IA (sentiment + temas detectados como Pills teal) + textarea "Respuesta sugerida (IA)" pre-llenada + editable + botón "Aprobar antes de publicar" (cambia a estado aprobado con check + "Publicar ahora" + nota "Revisión obligatoria"). StarChart SVG personalizado (4 semanas demo con gradiente gold, puntos con valor, eje X con etiquetas semana, panel lateral con media 4.46★ y tendencia). IA Copilot: chat transcript con burbujas user (gold) / ai (glass) mostrando texto + grid 3 cols (Fuente/Actualizado/Confianza%) + acciones sugeridas como pills amber "revisar antes de ejecutar". Chips predefinidos (3 consultas) con respuestas demo ricas (texto + fuente + actualizado + confianza + 1-2 acciones). Input libre + botón Preguntar (Enter envía). DataTable "Analytics disponibles" 12 filas (Métrica/Fuente/Frecuencia con Mono). GlassCard gold "Reglas de IA" GoldList 11 (fuente siempre mostrada, fecha actualización, confianza, acciones con revisión humana, registro ejecutadas, prompts versionados, límites por plan, redacción PII, kill switch, fallback determinista, aprobación humana para sensible). Callout warn "IA propone, humano decide lo sensible" (nunca ejecuta sensible sin aprobación, fallback garantiza operación).
- React keys: cada celda en arrays DataTable rows usa key semántico por columna (c-nombre/c-email/c-phone/c-visitas/...; cmp-trigger/cmp-cond/...; m-ocu/m-ing/...); items de GoldList usan key `crm-r1`/`ar-${i}`/`ex-${i}`; lista de clientes `key={c.id}`; reseñas `key={r.id}`; nodos del canvas usan `React.Fragment key={n.id}` para envolver card + flecha; chips copilot `key={c.id}`; mensajes chat `key={m.id}`. Sin colisiones.
- Componentes auxiliares: `Mono` (inline code dorado/turquesa/muted), `DEMO_BADGE` (badge amber "demo"), `ConsentBadge` (channel + ok=true/false), `StatusDot` (success/failed/pending con dot pulse), `SentimentPill` (positive/neutral/negative), `StarChart` (SVG con gradiente + puntos + media). Helper `nextNodeId()` para ids únicos al añadir/clonar nodos (evita colisiones React key al cargar plantilla).
- Lint inicial: 6 errores `react/jsx-key` por spans sin key en GoldList items de "Reglas de CRM". Añadido `key="crm-r1"`..`crm-r6`. Lint final: 0 errores, 0 warnings. TypeScript: 1 error inicial en línea 1114 (`Type 'string' is not assignable to type '"success" | "failed" | "pending"'`) por array `EXECUTIONS` sin tipado explícito — solucionado declarando `const EXECUTIONS: { id: string; flow: string; status: "success" | "failed" | "pending"; time: string; duration: string }[]`. `bunx tsc --noEmit` ya no reporta errores en p-d.tsx.
- Dev log: solo reporta module-not-found para p-c y p-e (otros subagentes paralelos). Mi p-d resuelve limpiamente.

## Stage Summary
- Archivo creado: `src/components/rp/sections-producto/p-d.tsx` (~1840 líneas, 3 exports interactivos).
- 3 secciones navegables vía NAV_PRODUCTO (items 12, 13, 14): CRM, Automatizaciones, Reputación.
- Componentes React con `useState` reales y genuinamente interactivos:
  - **CRM**: lista clickable + panel de detalle dinámico que cambia por cliente seleccionado (contacto, historial, preferencias, consentimientos, tags, notas).
  - **Automatizaciones**: paleta añade nodos, plantillas reemplazan flujo, click selecciona y edita config en textarea, toggle activar/pausar, botones duplicar/simular con feedback visual.
  - **Reputación**: lista de reseñas clickable + panel de detalle con textarea editable + botón aprobar con estado; chat IA con chips predefinidos e input libre, burbujas con fuente/actualizado/confianza/acciones.
- 3 DataTables (15 + 15 + 12 = 42 filas) con keys estables y únicas por celda.
- 3 GlassCards gold (Reglas de CRM, Ejemplos de automatizaciones, Reglas de IA) con GoldList.
- 3 Callouts warn (Consentimiento obligatorio, Aprobación humana para lo sensible, IA propone humano decide).
- 1 SVG chart personalizado (StarChart con gradiente gold, 4 semanas, media calculada).
- Contenido 100% en español, dark theme premium (dorado #D4AF37, turquesa #3DD6C9, glassmorphism, rp-glow-gold).
- DEMO_BADGE amber visible en cada componente interactivo y mini-sección.
- Lint limpio (0 errores, 0 warnings). TypeScript limpio en p-d.tsx. Sin bloqueadores.
