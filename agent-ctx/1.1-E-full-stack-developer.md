# Task 1.1-E — full-stack-developer

## Task
Construir el bloque E de Fase 1.1 (Arquitectura Enterprise) como 3 componentes en `src/components/rp/sections-fase1/f1-e.tsx`: `Fase1Eventos`, `Fase1Threat`, `Fase1Observabilidad`.

## Work Log
- Leí worklog previo: infra Fase 1.1 lista (mermaid.tsx, primitives con Risk/Callout/Code, phase-store, nav con 24 items, page.tsx wrapper client). Bloques A, B, C ya entregados por subagentes paralelos. Pendiente: D, E (este), F.
- Revisé `primitives.tsx` (firma exacta de Section/GlassCard/Stat/Pill/H3/Lead/DataTable/GoldList/KV/Risk/Callout/Code), `mermaid.tsx` (client, theme dark+gold+teal, fallback de error) y `f1-b.tsx` (patrones de uso de DataTable con keys estables y GlassCard con GoldList).
- Creé `src/components/rp/sections-fase1/f1-e.tsx` con tres exports:
  - **`Fase1Eventos`** (id=f1-eventos, index 15):
    - Mermaid flowchart LR del pipeline de eventos (Caso de uso → TX D1 → Dispatcher → Queue → 5 consumidores + DLQ).
    - `Code` lang=json con el envelope canónico (event_id ULID, event_type, event_version, occurred_at ISO, organization_id, location_id, cell_id, actor_id, actor_effective_id, correlation_id, causation_id, source, payload).
    - DataTable "Consumidores: garantías obligatorias" (8 filas: idempotencia, tolerancia a duplicados, fuera de orden, backoff+jitter, límite de reintentos, circuit breaker, DLQ, trazabilidad) con Risk alto/medio.
    - GlassCard gold "Webhooks salientes" (8 items: HMAC-SHA256 rotativo, timestamp anti replay 5min, backoff, at-least-once idempotente, webhook_deliveries, pausa/kill, eventos seleccionables, scope por org).
    - GlassCard default "Workflows para procesos largos" (7 items: onboarding, importaciones, exportaciones, campañas, conciliaciones, migración entre shards, restauración de backup).
    - Callout ok "Outbox = atomicidad" — same-transaction D1, at-least-once sin double-write, idempotencia final en consumidor.
  - **`Fase1Threat`** (id=f1-threat, index 16):
    - DataTable "Threat model (STRIDE)" 5 columnas (Categoría STRIDE | Amenaza | Control preventivo | Detectivo | Recuperación), 6 filas (Spoofing, Tampering, Repudiation, Information Disclosure, Denial of Service, Elevation of Privilege).
    - GlassCard gold "Controles transversales de seguridad" (13 items: CSP nonces, HSTS, CSRF, XSS, SQLi parametrizadas, SSRF allowlist, Zod, secretos fuera de código/D1/KV/R2/FE, TLS+R2 SSE, SAST+secret+DAST, supply chain, pentest pre-prod, separación prod vs test).
    - DataTable "Controles por superficie" 3 columnas (Superficie | Controles | Riesgo), 7 filas (API pública alto, Widget medio, Dashboard alto, Super Admin crítico, Webhooks entrantes alto, Integraciones alto, IA alto).
    - GlassCard "Respuesta a incidentes" en grid 5 columnas (Detectar/Contener/Erradicar/Recuperar/Aprender) con GoldList por columna.
    - Callout warn "Sin RLS nativa" — SQLite/D1 sin RLS, aislamiento en app layer (Tenant Enforcement Layer + constraints compuestos + tests IDOR en CI).
    - Stat row 4 (RPO ≤15min gold, RTO ≤2h gold, SLO núcleo 99.9% teal, Pentest pre-prod fg).
  - **`Fase1Observabilidad`** (id=f1-observabilidad, index 17):
    - DataTable "Pilares de observabilidad" 3 columnas (Pilar | Implementación | Notas), 11 filas (logs estructurados, métricas, trazas distribuidas, audit logs, alertas, SLO/SLA, status page, gestión de incidentes, redacción PII, retención, coste por org).
    - Mermaid flowchart LR del pipeline de observabilidad (Workers/DO/Queues → Logs/Métricas/Trazas → Warehouse+R2 → Dashboards/Alertas → On-call; vía separada Audit log D1 → R2 retención larga).
    - GlassCard gold "Correlation ID obligatorio" (6 items: gen en frontera, propagación a queues/workflows/DO/webhooks, audit log, ai_requests, reconstrucción journey, cliente lo recibe en errores).
    - GlassCard default "Control de costes por organización" (10 items: taggeo org_id, requests Workers, lecturas/escrituras D1, R2, KV, Queue, inferencias IA, agregación diaria → usage_records, comparación con entitlements, feed a billing+Super Admin).
    - Callout info "SLO y error budget" — 99.9% núcleo (reservas + floor ops por local), error budget ~43 min/mes, freeze de non-essential deploys si consumo >50%, SLO mide éxito visible para el usuario (no solo uptime), ventana 28 días.
- Honestidad técnica sobre límites Cloudflare: Workers Logs retención acotada, Analytics Engine límites de cardinalidad → export a warehouse para retención larga y consultas ad-hoc; sin distributed tracing nativo → correlation_id propagation como estrategia; D1 sin RLS → mitigado en app layer.
- React keys: cada celda en arrays `rows` usa key `${id}-${field}` (ej. `${g.id}-gar`, `${r.id}-prev`, `${p.id}-i`) sin colisiones; items de GoldList usan key semántica (ej. `wh-hmac`, `cid-gen`, `cost-tag`).
- Lint: `bunx eslint src/components/rp/sections-fase1/f1-e.tsx` → 0 errores, 0 warnings. `bun run lint` global → limpio. Dev log confirma que `f1-e` resuelve; el único module-not-found restante es `f1-f` (otro subagentes paralelo, no mi responsabilidad).

## Stage Summary
- Archivo creado: `src/components/rp/sections-fase1/f1-e.tsx` (~580 líneas, 3 exports).
- 2 diagramas Mermaid válidos (event pipeline LR, observability pipeline LR) con theme dark+gold+teal del proyecto.
- 4 DataTables (8 + 6 + 7 + 11 filas) con keys estables y únicas por celda.
- 5 GlassCards (gold webhooks salientes, default workflows, gold controles transversales, default respuesta a incidentes con grid 5 cols, gold correlation ID, default control de costes).
- 3 Callouts (ok outbox atomicidad, warn sin RLS nativa, info SLO y error budget).
- 1 Stat row (RPO/RTO/SLO/Pentest).
- 1 Code block (JSON envelope canónico).
- Contenido 100% en español, técnicamente preciso sobre primitivas Cloudflare (D1 sin RLS, Workers Logs/Analytics Engine limits, correlation_id como estrategia de tracing).
- Lint limpio. Sin bloqueadores. Pendiente de otros subagentes: f1-d (auth/impersonation/realtime) y f1-f (testing/cicd/escala/costes/backlog/criterios/adrs).
