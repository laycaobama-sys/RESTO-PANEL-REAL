# Task 1.2-D — full-stack-developer

## Task
Construir el bloque D de Fase 1.2 (Core Platform) como 4 componentes en
`src/components/rp/sections-fase2/f2-d.tsx`: `Fase2Seguridad`, `Fase2Observabilidad`,
`Fase2Datos`, `Fase2Contratos`.

## Files created
- `/home/z/my-project/src/components/rp/sections-fase2/f2-d.tsx` (~470 líneas, 4 exports)

## What was built

### Fase2Seguridad (id=f2-seguridad, index 16)
- Intro densa: cifrado tránsito/reposo, secretos, MFA, sesiones, CSRF/XSS/SSRF/SQLi, rate limiting,
  passwords, backups, eliminación/anonimización, consentimiento, retención, GDPR, respuesta a
  incidentes, acceso temporal de soporte.
- DataTable "Controles de seguridad" — 18 filas (Control / Implementación / Risk).
  Última fila "Acceso temporal de soporte" marcada como `crítico`.
- GlassCard gold "GDPR como restricción" — GoldList 8.
- GlassCard default "Privacidad por diseño" — GoldList 6.
- Callout warn "Sin RLS → disciplina de repos".

### Fase2Observabilidad (id=f2-observabilidad, index 17)
- DataTable "Pilares de observabilidad" — 6 filas.
- DataTable "Alta disponibilidad" — 9 filas con Risk.
- Mermaid flowchart LR del bucle HA+obs (Workers/DO/Queues → Logs/Métricas/Trazas → Warehouse/R2
  → Dashboards + Alertas → On-call → Runbooks → Mitigación; rama Health checks → Status page).
- GlassCard gold "Degradación controlada" — GoldList 6.
- Stat row 4: SLO 99.9% (gold), RPO ≤15min (teal), RTO ≤2h (fg), Error budget 43m/mes (gold).

### Fase2Datos (id=f2-datos, index 18)
- Mermaid erDiagram con 24 relaciones (organizations + restaurants + locations + floors + zones +
  tables + reservations + customers + history + preferences + tags + members + roles + menus +
  items + employees + shifts + campaigns + automations + ai_requests + audit_logs + notifications
  + subscriptions + plans).
- DataTable "Convenciones del modelo" — 11 filas.
- DataTable "Propiedad de tablas por dominio" — 15 dominios con sus tablas principales.
- Callout info "Propiedad lógica, no física (MVP)".

### Fase2Contratos (id=f2-contratos, index 19)
- Code block lang=typescript con los contratos del brief:
  - `OrgContext` (Zod + type)
  - `CreateReservationInput` + interfaz `CreateReservation`
  - `ReservationCreatedEvent` (event_version literal, pii_redacted)
  - `NotificationSender` (port)
  - `AuthzDecision` + `Authorizer`
- GlassCard gold "Reglas de contratos" — GoldList 5.
- Callout ok "Contratos = futura extracción sin reescritura".

## Patterns
- Helper local `Mono({children})` para inline code dorado.
- React keys en DataTables: `${id}-${field}` por celda (estables y únicas por fila).
- Mermaid: erDiagram con `A ||--o{ B : rel`; flowchart LR con strings citados, cylinders
  `[(Warehouse / R2)]`, multi-nodo. Compatible con securityLevel loose + theme dark+gold+teal.
- Honestidad Cloudflare: D1/SQLite sin RLS (riesgo #1 de fuga), celda D1 con redirección solo
  post-Etapa B, Time Travel ≠ backup, correlation_id propagation en ausencia de tracing nativo.

## Lint
- `bunx eslint src/components/rp/sections-fase2/f2-d.tsx` → 0 errores, 0 warnings.
- `bun run lint` global → 0 errores.
- Dev log: único module-not-found remanente es `f2-e` (otro subagente paralelo); `f2-d` resuelve.

## Blockers
Ninguno para este bloque. Pendiente de otros subagentes: f2-a, f2-b, f2-c, f2-e.
